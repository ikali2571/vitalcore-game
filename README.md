# VitalCore Progress Website

Upload all files and the complete `assets` folder to the root of your GitHub repository. Replace the existing `index.html`, `styles.css`, and `script.js`.

The screenshot panels are placeholders. Replace them with captures from Unity when ready. Replace the `#` in the Join Discord link with your Discord invite URL.
